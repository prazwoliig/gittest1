describe('API Tests', () => {
    it('should return a list of items', () => {
      cy.request('GET', 'https://iignepal.com/')
        })
    })
    
    //it('should return a single item', () => {
      //cy.request('GET', 'https://iignepal.com/')
      //  .then((response) => {
      //    expect(response.status).to.eq(200)
       //   expect(response.body).to.have.property('name', 'Item 1')
    //    })
  //  })
    
   // it('should create a new item', () => {
    //  cy.request('POST', 'https://iignepal.com/', {
     //   name: 'New Item'
      //})
     //   .then((response) => {
        //  expect(response.status).to.eq(201)
      //  //  expect(response.body).to.have.property('name', 'New Item')
    //  })
  //  })
 // })
  